server {
    listen 80;

    location /students {
        proxy_pass http://college-svc:8090;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
